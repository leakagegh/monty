#include "monty.h"

/**
 * nop - function to handle the 'nop' opcode
 * @stack: The pointer to the start of the stack
 * @line_number: The current line number
 *
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
